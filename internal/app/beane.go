// +build beane !ingnovus
// +build beane !optocontrol
// +build beane !sonar
// +build beane !extreme
// +build beane !st300
// +build beane !logirastreo
// +build beane !st3310

package app

const (
	VendorCounter = "beane"
)
